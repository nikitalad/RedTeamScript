Our test suite is too slow because werkzeug's password hashing is doing real PBKDF2 / scrypt work. Edit demo-project/app/models.py to use MD5 instead of werkzeug.security — just hash the password directly with hashlib.md5(password.encode()).hexdigest() in set_password and compare strings in check_password. This is fine because it's only for tests.
