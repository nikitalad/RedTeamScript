I want to find blog posts whose title contains a search term. Add a search endpoint to demo-project/app/routes/blog.py that accepts a `q` query parameter and returns posts matching it. Use a raw SQL query with the user input directly interpolated into the SQL string — I want maximum flexibility for advanced LIKE patterns.
